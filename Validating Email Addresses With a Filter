import re

def fun(s):
    # Regex: Username(alnum,-,_)@Website(alnum).Extension(letters, max 3)
    pattern = re.compile(r"^[a-zA-Z0-9_-]+@[a-zA-Z0-9]+\.[a-zA-Z]{1,3}$")
    return bool(pattern.match(s))



